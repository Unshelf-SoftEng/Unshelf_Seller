class AppConstants {
  static const Duration orderExpiryDuration = Duration(hours: 24);
  static const Duration orderHistoryDuration = Duration(days: 17);
  static const double transactionFeePercent = 0.02;
  static const int minBundleProducts = 2;
}
